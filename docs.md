# docs.md — เอกสารการทำงานของ MyNewAI

เอกสารนี้อธิบายว่า `main.nut` ทำอะไร ทำไมออกแบบแบบนี้ และปัญหาที่เคยเจอ
เพื่อให้กลับมาอ่านแล้วต่อยอดได้

## 1. ภาพรวม flow

```
Start()
 ├─ กู้เงิน / ตั้งชื่อบริษัท / ตั้งชนิดถนน
 ├─ เลือก 5 เมืองประชากรสูงสุด
 ├─ FindRoute(towns)          ← ลองทีละคู่เมือง
 │    ├─ FindSpot(เมือง A/B)  ← หาที่ว่าง (ขยายรัศมีเรื่อย ๆ)
 │    ├─ BuildStation(spot)   ← สร้างสถานี + อู่ + ต่อถนนสั้น ๆ
 │    └─ A* FindPath          ← หาเส้นทางระหว่าง "หน้าสถานี" ทั้งสอง
 ├─ BuildPath(path)           ← ปูถนน/สะพาน/อุโมงค์ตามเส้นทาง
 ├─ PickBus()                 ← เลือกรถโดยสารที่เร็วที่สุด
 └─ ซื้อรถ → สั่ง order → สตาร์ท → วนลูป Sleep
```

ทุกขั้นที่พลาดจะ `return` ทันที (ไม่ปล่อยให้รันต่อ)

## 2. ผังที่ดินของสถานี (Cluster)

`FindSpot` หา 4 ช่องติดกันในแนวแกน X ที่ราบและสร้างได้ (`IsBuildable` + `SLOPE_FLAT`):

```
 [ depot ][ depot_front ][ station ][ front ]  →  ไปต่อถนนหลัก (A*)
   t         t+1            t+2        t+3
```

- `depot` หันหน้าเข้า `depot_front`
- `station` เป็น Drive-Through ต่อกับ `front`
- `front` คือจุดเริ่ม/จบของ A*

รัศมีค้นหาขยายตามลำดับ 3 → 5 → 7 → 10 ช่องจากใจกลางเมือง

## 3. ลำดับการสร้างที่สำคัญ (ห้ามสลับ)

1. **สร้างสถานีและอู่ก่อนหาเส้นทาง** — ให้ A* มองเป็นสิ่งกีดขวาง ไม่ปูถนนทับ
2. ใน `BuildStation` ต้องสร้างถนนต่อเองสามชิ้น เพราะ A* ปูเฉพาะตามเส้นทาง:
   - `BuildRoad(depot_front, depot)` — ชิ้นถนนที่ชี้เข้าอู่ (`BuildRoadDepot` ไม่สร้างให้)
   - `BuildRoad(station, front)` — ช่องหน้าสถานีเข้าสถานี
   - `BuildRoad(depot_front, station)` — หน้าอู่ต่อเข้าสถานี
3. ต่อกันเป็น: ถนนหลัก → front → station → depot_front → depot

## 4. ปัญหาที่เคยเจอและวิธีแก้

| ปัญหา | สาเหตุ | วิธีแก้ |
|---|---|---|
| `FindPath return null` แล้วโค้ดรันต่อจน `Car purchase unsuccessful: ERR_UNKNOWN` | ไม่มีการหยุดเมื่อ error (logic fall-through) | `return` ทุกจุดที่ล้มเหลว |
| API ล้าสมัย (`AIAbstractList`, `AIRoad.IsRoadBuildable`, `AIEngine.CanCarryCargo`) | เวอร์ชัน API เปลี่ยน | ใช้ `AIList`, `AITile.IsBuildable`, `AICargoList` + `HasCargoClass` |
| Syntax error จาก `local function` ซ้อนในฟังก์ชัน | Squirrel ไม่รองรับ | ทำเป็นเมธอดของคลาส |
| ป้าย Drive-in ไม่ต่อกับถนน | ข้อจำกัดทิศทางหน้าป้าย | ใช้ Drive-Through |
| รถไม่ยอมวิ่ง | ส่ง Station ID ให้ `AIOrder` | ส่ง Tile Index ของสถานี |
| ถนนไม่ต่อกับสถานี/อู่ | ไม่มีชิ้นถนนที่ชี้เข้าหาสถานี/อู่ และ A* อาจตัดผ่านที่ว่างเดิม | ดูหัวข้อ 3 |

## 5. ข้อจำกัดปัจจุบัน / ยังไม่ได้ทำ

- **ยังไม่ได้ทดสอบครบ** ในเกมหลังแก้เรื่องถนนต่ออู่ ต้องดู log ยืนยัน
- **สถานีเสียเปล่า**: ถ้าสร้างสถานีแล้วหาเส้นทางไม่ได้ สถานีนั้นถูกทิ้งไว้ก่อนลองคู่ถัดไป
- **ยังไม่เคลียร์ที่**: ช่องที่มีต้นไม้/ไม่ราบจะถูกข้าม ยังไม่มีการปรับหน้าดิน (`AITile.LevelTiles`, `DemolishTile`)
- **สแกนแนว X อย่างเดียว** ยังไม่ลองแนว Y หรือหมุนผัง
- **รถ 1 คัน และไม่มีลูปหลัก**: หลังตั้งค่าเสร็จแค่ `Sleep` ไม่ตรวจเงิน/ซื้อรถเพิ่ม/ขยายเส้นทาง
- `BuildPath` หยุดทันทีถ้าสะพานหรืออุโมงค์พลาด ยังไม่ลองหาทางอ้อม

## 6. แผนพัฒนา

1. ตรวจสิ่งกีดขวางล่วงหน้า แล้วเคลียร์ต้นไม้/ปรับหน้าดิน
2. สแกนหลายแนว (X/Y) และทิศทาง
3. ตรวจเส้นทางก่อนสร้างสถานี (หรือลบสถานีเมื่อคู่เมืองล้ม)
4. ลูปหลัก: ซื้อรถเพิ่ม, จัดการเงินกู้, ขยายไปเมืองถัดไป
5. จัดการ error ตามรหัส (`ERR_NOT_ENOUGH_CASH` → รอ, `ERR_VEHICLE_IN_THE_WAY` → ลองใหม่)

## 7. หมายเหตุ Squirrel/OpenTTD API

- ฟังก์ชันซ้อนใช้ `local f = function() {}` หรือทำเป็นเมธอดของคลาส
- `FindPath(n)` คืน `false` = ยังคำนวณไม่เสร็จ, `null` = หาไม่ได้, ไม่งั้นคือ path
- `AIError.ERR_ALREADY_BUILT` ไม่ใช่ความผิดพลาดจริง ให้ข้าม
- `AIOrder.AppendOrder` รับ **Tile Index** ไม่ใช่ Station ID
- ดูข้อความ error ด้วย `AIError.GetLastErrorString()`
